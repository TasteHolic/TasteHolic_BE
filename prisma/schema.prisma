generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql" // 혹은 다른 DB 타입 (e.g., "postgresql")
  url      = env("DATABASE_URL")
}

model User {

  id       Int      @id @default(autoincrement())
  email    String   @unique
  password String
  nickname String
}